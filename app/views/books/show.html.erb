<p id="notice"><%= notice %></p>

<p>
  <strong><%= t 'common.title' %>:</strong>
  <%= @book.title %>
</p>

<p>
  <strong><%= t 'common.memo' %>:</strong>
  <%= @book.memo %>
</p>

<p>
  <strong><%= t 'common.author' %>:</strong>
  <%= @book.author %>
</p>

<p>
  <strong><%= t 'common.picture' %>:</strong>
  <%= image_tag(@book.picture_url) if @book.picture.present? %>
</p>

<%= link_to t('common.edit'), edit_book_path(@book) %> |
<%= link_to t('common.back'), books_path %>
